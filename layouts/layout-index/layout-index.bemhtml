block('layout-index')(

    js()(true),

    content()(function() {
        return {
            block : 'button',
            mods : { theme : 'islands', size : 'xl' },
            text : 'review'
        };
    })

);
